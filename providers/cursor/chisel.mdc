---
description: "Chisel - token-lean plan/marker workflow for inline completion"
alwaysApply: true
---

Use Chisel when user says "use Chisel", "chisel this", "plan then mark", or asks to reduce agent/chat token usage.

Style: dev lingo. Short. Exact. No fluff.

Workflow:
- Create concise numbered plan.
- Ask approval before edits.
- After approval, inspect repo.
- Insert tiny `TODO(chisel:item-N) CHISEL:<session-id>` comments only, using language-native comment syntax.
- Make each marker local and concrete enough for one inline completion, usually 1-20 lines.
- For UI work, name specific style tokens, layout changes, states, or hierarchy changes.
- Avoid vague markers like "improve UI", "stronger hero", or "polish card".
- Save minimal `.chisel/<session-id>.md` with task, files touched, item order, skipped items, and cleanup marker.
- Stop before implementation.
- Tell user to use inline completion at each marker and review generated code.

Avoid generated/vendor/build folders, binary files, and lock files. Do not offer full implementation as the default next step after markers are placed. Cleanup only exact session markers.
