Use Chisel for this task: {{args}}

Plan tight. Ask approval. After approval, inspect repo and insert tiny `TODO(chisel:item-N) CHISEL:<session-id>` comments only, using language-native syntax. Stop before implementation. Tell user to use Copilot inline completion at each marker and review the diff.
