Use Chisel for this task: {{args}}

Plan tight. Group plan items by method using a shared-decision vs separable-concerns test so intended marker count is visible before approval. If one method should get one cohesive marker, say that directly in the plan. Ask approval. Approval for the marker pass means comment insertion only, not implementation. Tiny mode max: 3 markers. Normal mode max: 8 markers, then report `skipped: marker cap reached`. After approval, inspect repo and insert tiny two-line CHISEL/TODO marker blocks only, using language-native syntax. Every CHISEL marker must be on its own line, and the TODO line must immediately follow it. Use one top-of-method marker for one shared design or behavior decision. Use multiple markers inside a method only when the concerns are genuinely separate. Save `.chisel/<session-id>.md` and `.chisel/<session-id>.json` receipts. Run `chisel verify <session-id>` or the local equivalent and include the output before declaring the pass complete. If verify reports FAIL, say so explicitly. Stage mode is opt-in only. Only stage old code when the user explicitly asks to, never wrap already-commented code in another block comment, and only keep the staged version if syntax check passes. Stop before implementation. Tell user to use inline completion or implement by hand at each marker and review the diff.
