Use Chisel for this task: {{args}}

Plan tight. Ask approval. Approval for the marker pass means comment insertion only, not implementation. Tiny mode max: 3 markers. Normal mode max: 8 markers, then report `skipped: marker cap reached`. After approval, inspect repo and insert tiny two-line CHISEL/TODO marker blocks only, using language-native syntax. Every CHISEL marker must be on its own line, and the TODO line must immediately follow it. Save `.chisel/<session-id>.md` and `.chisel/<session-id>.json` receipts. Stop before implementation. Tell user to use inline completion or implement by hand at each marker and review the diff.
