Use Chisel for this task: {{args}}

Plan tight. Ask approval. After approval, inspect repo and insert tiny `TODO(chisel:item-N) CHISEL:<session-id>` comments only, using language-native syntax. Save `.chisel/<session-id>.md` and `.chisel/<session-id>.json` receipts. Stop before implementation. Tell user to use inline completion or implement by hand at each marker and review the diff.
