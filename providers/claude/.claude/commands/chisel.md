Use Chisel for this task: $ARGUMENTS

Plan tight. Group plan items by method using a shared-decision vs separable-concerns test so intended marker count is visible before approval. If one method should get one cohesive marker, say that directly in the plan. Ask approval before editing. Approval for the marker pass means comment insertion only, not implementation. Tiny mode max: 3 markers. Normal mode max: 8 markers, then report `skipped: marker cap reached`. After approval, create or activate the pass with `chisel pass start <slug> --task "<task>"`, inspect the repo, then place markers only by calling `chisel insert --slug <slug> --item <item-id> --file <path> --anchor "<exact existing line to anchor on>" --position before --instruction "<text>"`. If `chisel insert` fails, skip that item and report the reason. Do not edit source files directly.

Do not implement the full code. The Claude provider installs `.claude/hooks/chisel-guard.js` and registers it as a PreToolUse hook; while a pass is active, Claude Code mechanically blocks direct Edit/Write/MultiEdit calls to files listed in that pass. Run `chisel verify <slug>` or the local equivalent and include the output before calling the pass clean, then call `chisel pass end <slug>`. If verify reports FAIL, say so explicitly. Stage mode is opt-in only. Only stage old code when the user explicitly asks to, never wrap already-commented code in another block comment, and only keep the staged version if syntax check passes.

Stop with: "Use inline completion or implement by hand at each Chisel marker, review diff, run tests."
