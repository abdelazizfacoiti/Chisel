Use Chisel for this task: $ARGUMENTS

Plan tight. Group plan items by method using a shared-decision vs separable-concerns test so intended marker count is visible before approval. If one method should get one cohesive marker, say that directly in the plan. Ask approval before editing. Approval for the marker pass means comment insertion only, not implementation. Tiny mode max: 3 markers. Normal mode max: 8 markers, then report `skipped: marker cap reached`. After approval, inspect the repo and insert tiny two-line CHISEL/TODO marker blocks only, using language-native syntax. Every CHISEL marker must be on its own line, and the TODO line must immediately follow it. Use one top-of-method marker for one shared design or behavior decision. Use multiple markers inside a method only when the concerns are genuinely separate.

Do not implement the full code. Save minimal `.chisel/<session-id>.md` and `.chisel/<session-id>.json` receipts with task, files touched, item order, skipped items, and cleanup marker. Run `chisel verify <session-id>` or the local equivalent and include the output before calling the pass clean. If verify reports FAIL, say so explicitly. Stage mode is opt-in only. Only stage old code when the user explicitly asks to, never wrap already-commented code in another block comment, and only keep the staged version if syntax check passes.

Stop with: "Use inline completion or implement by hand at each Chisel marker, review diff, run tests."
